const main = async () => {
    console.log('I created a new page!');
};

window.addEventListener('DOMContentLoaded',main);

function loadImage(){
    if(confirm("Howdy Pardner")==true){
        al
    }
}